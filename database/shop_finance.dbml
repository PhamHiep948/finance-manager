Project handmade_finance {
  database_type: "PostgreSQL"
  Note: '''
    Thu/chi cơ bản. Schema shop_finance.
    Không lưu chi tiết sản phẩm, discount, shipping, tax.
  '''
}

Enum user_role {
  ADMIN
  SHOP_OWNER
  EMPLOYEE
  VIEWER
}

Enum data_source {
  MANUAL
  EXCEL_IMPORT
}

Enum import_type {
  INCOME
  EXPENSE
}

Enum import_status {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
}

Enum audit_action {
  INSERT
  UPDATE
  DELETE
}

TableGroup users {
  app_users
}

TableGroup income {
  income_categories
  incomes
}

TableGroup expense {
  expense_categories
  expenses
}

TableGroup shared {
  import_batches
  attachments
  audit_logs
}

Table app_users [note: "Người dùng; 4 vai trò, không có bảng permission"] {
  id bigint [pk, increment]
  username varchar(100) [not null]
  email varchar(255)
  password_hash text [not null]
  full_name varchar(255) [not null]
  role user_role [not null, default: "EMPLOYEE"]
  is_active boolean [not null, default: true]
  last_login_at timestamptz
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
  deleted_at timestamptz
}

Table import_batches [note: "Theo dõi từng lần import Excel"] {
  id bigint [pk, increment]
  import_type import_type [not null]
  original_file_name varchar(500) [not null]
  stored_file_path text
  status import_status [not null, default: "PENDING"]
  total_rows integer [not null, default: 0]
  success_rows integer [not null, default: 0]
  failed_rows integer [not null, default: 0]
  error_details jsonb
  imported_by bigint
  created_at timestamptz [not null, default: `now()`]
  completed_at timestamptz
}

Table income_categories [note: "Phân loại khoản thu"] {
  id bigint [pk, increment]
  name varchar(150) [not null]
  description text
  is_active boolean [not null, default: true]
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
  deleted_at timestamptz
  created_by bigint
}

Table incomes [note: "Một dòng = một khoản tiền vào. amount là số tiền cuối cùng."] {
  id bigint [pk, increment]
  income_date date [not null]
  description text [not null]
  income_category_id bigint [not null]
  amount numeric(18,2) [not null]
  currency_code varchar(3) [not null]
  reference_code varchar(150)
  source data_source [not null, default: "MANUAL"]
  import_batch_id bigint
  note text
  created_by bigint
  updated_by bigint
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
  deleted_at timestamptz
  deleted_by bigint
}

Table expense_categories [note: "Phân loại khoản chi"] {
  id bigint [pk, increment]
  name varchar(150) [not null]
  description text
  is_active boolean [not null, default: true]
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
  deleted_at timestamptz
  created_by bigint
}

Table expenses [note: "Một dòng = một khoản tiền ra"] {
  id bigint [pk, increment]
  expense_date date [not null]
  description text [not null]
  expense_category_id bigint [not null]
  amount numeric(18,2) [not null]
  currency_code varchar(3) [not null]
  payee varchar(255)
  source data_source [not null, default: "MANUAL"]
  import_batch_id bigint
  note text
  created_by bigint
  updated_by bigint
  created_at timestamptz [not null, default: `now()`]
  updated_at timestamptz [not null, default: `now()`]
  deleted_at timestamptz
  deleted_by bigint
}

Table attachments [note: "Metadata chứng từ; file ngoài Postgres. Đúng một trong income_id / expense_id"] {
  id bigint [pk, increment]
  income_id bigint
  expense_id bigint
  original_name varchar(500) [not null]
  storage_path text [not null]
  mime_type varchar(150)
  file_size_bytes bigint
  uploaded_by bigint
  uploaded_at timestamptz [not null, default: `now()`]
}

Table audit_logs [note: "Trigger INSERT/UPDATE/DELETE; actor từ SET LOCAL app.current_user_id"] {
  id bigint [pk, increment]
  table_name varchar(100) [not null]
  record_id bigint
  action audit_action [not null]
  actor_user_id bigint
  old_data jsonb
  new_data jsonb
  changed_at timestamptz [not null, default: `now()`]
  request_id uuid
  ip_address varchar
}

Ref: import_batches.imported_by > app_users.id [delete: set null]
Ref: income_categories.created_by > app_users.id [delete: set null]
Ref: incomes.income_category_id > income_categories.id
Ref: incomes.import_batch_id > import_batches.id [delete: set null]
Ref: incomes.created_by > app_users.id [delete: set null]
Ref: incomes.updated_by > app_users.id [delete: set null]
Ref: incomes.deleted_by > app_users.id [delete: set null]
Ref: expense_categories.created_by > app_users.id [delete: set null]
Ref: expenses.expense_category_id > expense_categories.id
Ref: expenses.import_batch_id > import_batches.id [delete: set null]
Ref: expenses.created_by > app_users.id [delete: set null]
Ref: expenses.updated_by > app_users.id [delete: set null]
Ref: expenses.deleted_by > app_users.id [delete: set null]
Ref: attachments.income_id > incomes.id [delete: cascade]
Ref: attachments.expense_id > expenses.id [delete: cascade]
Ref: attachments.uploaded_by > app_users.id [delete: set null]
Ref: audit_logs.actor_user_id > app_users.id [delete: set null]
